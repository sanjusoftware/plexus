# --- H2 Database Configuration (Test Context Only) ---
# Use a unique in-memory database name for the test context
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

app.security.system-bank-id=SYSTEM

# --- JPA/Hibernate Settings for Test ---
# Use 'create-drop' to ensure the schema is built for the test context
# and dropped when the context shuts down (after all tests finish).
spring.jpa.hibernate.ddl-auto=create-drop

# --- Disable Seeder Setting (Redundant, but good practice) ---
app.seed.enabled=false
# Disable standard Spring Boot SQL initialization scripts
spring.sql.init.mode=never

# --- Security / OIDC Mocking ---
# Providing a JWK Set URI directly skips the OIDC discovery (.well-known) phase.
# This prevents the UnknownHostException for plexus-bankengine.com during startup.
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:9090/default/jwks
# We still set the issuer to satisfy internal validation if a token is ever parsed
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://plexus-bankengine.com/auth